using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Dfine
{
    public enum StageScene
    {
        Start,
        Lobby,
        Clear,
    }
   public enum State
    {
        Idle,
        Move,
        Attack
    }
}
